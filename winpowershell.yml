---
- name: run a powershell script
 # hosts: all
  hosts: windows
  gather_facts: false
  tasks:
    - name: run a powershell script
      script: scripts/mem.ps1
#      register: out
#    - debug: var=out
